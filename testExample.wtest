import example.*

describe "group of tests" {
  var nave = new NaveBaliza(velocidad = 10, direccion = 0, combustible = 500)
  test "pepita has initial energy" {
    nave.recibirAmenaza()
    assert.equals(10, nave.direccion())
  }

}